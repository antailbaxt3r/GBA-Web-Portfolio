/**
 * The plain-HTML portfolio: same content, no game, no JavaScript.
 *
 * Generated at build time by the `portfolio-static-mirror` plugin in
 * vite.config.ts, which emits `minimal/index.html`. Keep the two in step —
 * the trailing slash matters, since it is a directory index and without it
 * the host has to issue a redirect first.
 */
export const MINIMAL_URL = '/minimal/';

/** Colour used for the "switch site" entry, to set it apart from the rest. */
export const SWITCH_TINT = 0x4890d8;

export const PAUSE_SWITCH_TINT = 0xD4ECFF;
